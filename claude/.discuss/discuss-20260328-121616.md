# Discussion Record

## Context
- 时间：2026-03-28 12:16:16
- 耗时：约 3 分钟
- 用户原始需求：doc skill 足够好吗？type类型是不是太多了有点重叠冗余？
- 复杂度评估：低
- 讨论轮数：2 轮
- 参与角色：R5 第一性原理者、R2 极简主义者、R4 实用主义者、R7 用户代言人、R14 架构师

## 内容摘要

逐个审问 7 种 type 的存在理由后，确认 `inline`（行内注释）与 `api`（文档注释）高度重合——都是"扫描源文件补注释"，用户不区分两种注释类型。R7 为 `onboard` 辩护成功（受众不同于 readme）。最终合并 `inline` 进 `api`，type 从 7 精简为 6。

---

## 最终方案

合并 `inline` 进 `api`，`api` 扩展为"文档注释 + 行内注释一次扫描全覆盖"。其余 type（readme、changelog、onboard、summary）保留。
